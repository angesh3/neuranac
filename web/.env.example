# NeuraNAC Web Dashboard — Environment Variables
# Copy this file to .env and adjust as needed.

# API Gateway upstream URL (used by nginx proxy in Docker, or Vite dev proxy)
VITE_API_URL=http://localhost:8080/api/v1

# Port for the web container (Docker only)
WEB_PORT=3001
